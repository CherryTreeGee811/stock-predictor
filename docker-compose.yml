services:
  stock-predictor-api:
    build: .
    image: stock-predictor
    container_name: stock-predictor
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
