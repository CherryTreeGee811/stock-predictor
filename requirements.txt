# Stock Price Prediction Engine — Python Dependencies
# Install with: pip install -r requirements.txt

# --- Data fetching ---
yfinance

# --- Data processing ---
pandas
numpy

# --- Technical indicators (manual computation, no extra lib needed) ---

# --- Traditional ML ---
scikit-learn
xgboost

# --- Deep learning (LSTM) ---
tensorflow

# --- Hyperparameter tuning (included in scikit-learn) ---

# --- Visualization ---
matplotlib
seaborn

# --- Configuration ---
pyyaml
python-dotenv

# --- CLI / App ---
# (no extra dependency — uses built-in input() or argparse)

# --- Notebook (optional, for exploration.ipynb) ---
jupyterlab
